import React from "react"
import styled from "styled-components"

type Props = {}

export const QuizCreator: React.FC<Props> = () => {
  return <Wrapper>Quiz creator</Wrapper>
}

const Wrapper = styled.div``
